import React from 'react';

/* Components */
import Logo from '../../components/Logo/Logo';

const Header = () => {
    return (
        <div>
            <Logo/>
        </div>
    );
}

export default Header;
